const data = [11, "Abdullah Faisal", "Engineer", "New Delhi"];
let [id, name, dsg, address] = data;
console.log(`
    ID      : ${id}
    Name    : ${name}
    Dsg     : ${dsg}
    Address : ${address}
    `);
