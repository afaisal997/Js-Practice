// 3. 3rd Array constructor

// We can create array like this by using new keyword and using push() method.

let arr = new Array();

arr.push(12);
arr.push("world");
arr.push(10.1);

console.log(arr);

// We can create array also like this //directly push it inside the paranthesis.

let arr2 = new Array(11, "hello", 13.2);
console.log(arr2);
